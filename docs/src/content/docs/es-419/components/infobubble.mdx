---
title: InfoBubble
head:
  - tag: meta
    attrs:
      name: description
      content: Muestra ventanas de información ancladas a marcadores usando InfoBubble en MapConductor iOS SDK.
---

import InfoBubbleBasicExample from '~/components/components/infobubble/InfoBubbleBasicExample.astro';
import InfoBubbleInitializer from '~/components/components/infobubble/InfoBubbleInitializer.astro';
import InfoBubbleStylingExample from '~/components/components/infobubble/InfoBubbleStylingExample.astro';

`InfoBubble` renderiza contenido SwiftUI anclado a un `MarkerState`.

Usa una burbuja de información cuando un marcador necesite contenido SwiftUI visible y dinámico. La burbuja sigue al marcador en lugar de ser un callout nativo del proveedor.

<InfoBubbleBasicExample />

## Inicializador

<InfoBubbleInitializer />

Usa `extra` o tu propio modelo para controlar el contenido de la burbuja.

## Estilo

El estilo predeterminado le da a la burbuja un contenedor y una cola integrados. Para cambiar su apariencia, pasa `bubbleColor`, `borderColor`, `contentPadding`, `cornerRadius` o `tailSize` — los mismos parámetros que toma el `InfoBubble` de Android. Para dibujarlo todo tú mismo, cola incluida, usa `InfoBubbleCustom`:

<InfoBubbleStylingExample commentForLabel="Lugar seleccionado" />

Como el contenido es SwiftUI, puedes vincularlo a tu modelo de aplicación, al estado del marcador o al estado de selección.

## Ciclo de Vida

Renderiza el marcador y su `InfoBubble` en el mismo closure de contenido del mapa. Mantén el `MarkerState` estable mientras la burbuja esté visible para que el SDK pueda mantenerla anclada al mismo marcador.
