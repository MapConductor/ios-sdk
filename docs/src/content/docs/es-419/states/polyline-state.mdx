---
title: PolylineState
head:
  - tag: meta
    attrs:
      name: description
      content: PolylineState — actualiza dinámicamente los puntos y trazo de una polilínea en MapConductor iOS SDK.
---

import PolylineStateBasicExample from '~/components/states/polyline-state/PolylineStateBasicExample.astro';
import PolylineStateRouteUpdateExample from '~/components/states/polyline-state/PolylineStateRouteUpdateExample.astro';

`PolylineState` es un `ObservableObject` usado por `Polyline(state:)`.

Úsalo cuando una ruta, trayectoria o estilo de línea necesite actualizarse sin recrear la declaración de superposición.

## Propiedades públicas

- `id: String`
- `strokeColor: UIColor`
- `strokeWidth: Double`
- `geodesic: Bool`
- `points: [GeoPointProtocol]`
- `extra: Any?`
- `onClick: OnPolylineEventHandler?`

<PolylineStateBasicExample />

## Actualizaciones de ruta

Reemplaza el arreglo completo de puntos cuando se recalcule una ruta:

<PolylineStateRouteUpdateExample />

Usa `onClick` cuando el usuario deba poder seleccionar una ruta o inspeccionar metadatos de línea almacenados en `extra`.
