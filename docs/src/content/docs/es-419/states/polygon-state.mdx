---
title: PolygonState
head:
  - tag: meta
    attrs:
      name: description
      content: PolygonState — actualiza dinámicamente los puntos, relleno y borde de un polígono en MapConductor iOS SDK.
---

import PolygonStateBasicExample from '~/components/states/polygon-state/PolygonStateBasicExample.astro';
import PolygonStateHolesExample from '~/components/states/polygon-state/PolygonStateHolesExample.astro';

`PolygonState` es un `ObservableObject` usado por `Polygon(state:)`.

Úsalo cuando un área rellena cambie de forma, estilo, huecos, z-index o estado de selección después de su creación.

## Propiedades públicas

- `id: String`
- `strokeColor: UIColor`
- `strokeWidth: Double`
- `fillColor: UIColor`
- `geodesic: Bool`
- `zIndex: Int`
- `points: [GeoPointProtocol]`
- `holes: [[GeoPointProtocol]]`
- `extra: Any?`
- `onClick: OnPolygonEventHandler?`

Usa el canal alfa en `fillColor` o `strokeColor` para la transparencia.

<PolygonStateBasicExample />

## Actualizar huecos

`holes` es un arreglo de anillos dentro del polígono exterior:

<PolygonStateHolesExample />

Usa el canal alfa en `fillColor` para regiones translúcidas. Mantén `points` y `holes` en el orden de coordenadas requerido por tu modelo de datos.
