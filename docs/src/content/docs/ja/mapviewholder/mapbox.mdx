---
title: Mapbox MapViewHolder
head:
  - tag: meta
    attrs:
      name: description
      content: MapConductor iOS SDKのMapboxMapViewHolder — ネイティブMapboxインスタンスへのアクセス方法
---
import MapboxHolderExample from '~/components/mapviewholder/mapbox/MapboxHolderExample.astro';

`MapboxViewState.getMapViewHolder()` は `AnyMapViewHolder?` を返します。ネイティブビューは Mapbox の `MapView` にキャストでき、`holder.map` は `MapboxMap` にキャストできます。

<MapboxHolderExample />
