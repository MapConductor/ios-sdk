---
title: PolygonState
head:
  - tag: meta
    attrs:
      name: description
      content: PolygonState — dynamically update polygon points, fill, and stroke in MapConductor iOS SDK.
markdown:
  - headingLinks: false
---

import PolygonStateBasicExample from '~/components/states/polygon-state/PolygonStateBasicExample.astro';
import PolygonStateHolesExample from '~/components/states/polygon-state/PolygonStateHolesExample.astro';

`PolygonState` is an `ObservableObject` used by `Polygon(state:)`.

Use it when a filled area changes shape, style, holes, z-index, or selection state after creation.

## Public Properties

- `id: String`
- `strokeColor: UIColor`
- `strokeWidth: Double`
- `fillColor: UIColor`
- `geodesic: Bool`
- `zIndex: Int`
- `points: [GeoPointProtocol]`
- `holes: [[GeoPointProtocol]]`
- `extra: Any?`
- `onClick: OnPolygonEventHandler?`

Use alpha on `fillColor` or `strokeColor` for transparency.

<PolygonStateBasicExample />

## Updating Holes

`holes` is an array of rings inside the outer polygon:

<PolygonStateHolesExample />

Use alpha on `fillColor` for translucent regions. Keep `points` and `holes` in the coordinate order required by your data model.
