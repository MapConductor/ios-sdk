---
title: Polygon
head:
  - tag: meta
    attrs:
      name: description
      content: Draw polygon overlays with fill, stroke, and holes using Polygon in MapConductor iOS SDK.
---

import PolygonBasicExample from '~/components/components/polygon/PolygonBasicExample.astro';
import PolygonInitializer from '~/components/components/polygon/PolygonInitializer.astro';
import PolygonStateWithHolesExample from '~/components/components/polygon/PolygonStateWithHolesExample.astro';
import PolygonHolesExample from '~/components/components/polygon/PolygonHolesExample.astro';
import PolygonClickExample from '~/components/components/polygon/PolygonClickExample.astro';

`Polygon` draws a filled shape.

Use polygons for areas such as administrative boundaries, parcels, geofences, or custom regions. The first and last points do not need to be repeated; the provider implementation closes the shape.

<PolygonBasicExample />

## Initializers

<PolygonInitializer />

`PolygonState` also supports holes:

<PolygonStateWithHolesExample />

Use alpha on `fillColor` or `strokeColor` for transparency.

## Holes

Each hole is an array of coordinates inside the outer polygon:

<PolygonHolesExample />

## Interaction

Use `onClick` for selectable areas:

<PolygonClickExample />
