---
import { Code } from '@astrojs/starlight/components';

interface Props {
  precision?: number;
}

const {
  precision = 5,
} = Astro.props;

const code = `let value = tokyo.toUrlValue(precision: ${precision})`;
---

<Code code={code} lang="swift" />
