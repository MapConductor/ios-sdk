---
import { Code } from '@astrojs/starlight/components';

const code = `let routeLength = Spherical.computeLength(route)
let polygonArea = Spherical.computeArea(polygon)`;
---

<Code code={code} lang="swift" />
