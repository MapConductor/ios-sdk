---
import { Code } from '@astrojs/starlight/components';

const code = `let areaSquareMeters = Spherical.computeArea(polygonPoints)
let signedArea = Spherical.computeSignedArea(polygonPoints)`;
---

<Code code={code} lang="swift" />
