---
import { Code } from '@astrojs/starlight/components';

interface Props {
  latitude?: number;
  longitude?: number;
  zoom?: number;
}

const {
  latitude = 35.6812,
  longitude = 139.7671,
  zoom = 13,
} = Astro.props;

const code = `let state = MapLibreViewState(
    mapDesignType: MapLibreDesign.OsmBright,
    cameraPosition: MapCameraPosition(
        position: GeoPoint(latitude: ${latitude}, longitude: ${longitude}),
        zoom: ${zoom}
    )
)`;
---

<Code code={code} lang="swift" />
