---
import { Code } from '@astrojs/starlight/components';

interface Props {
  version?: string;
}

const {
  version = "{HEATMAP_MODULE_VERSION}",
} = Astro.props;

const code = `.package(url: "https://github.com/MapConductor/ios-heatmap", from: "${version}"),`;
---

<Code code={code} lang="swift" />
