---
import { Code } from '@astrojs/starlight/components';

interface Props {
  version?: string;
}

const {
  version = "{GEOJSON_MODULE_VERSION}",
} = Astro.props;

const code = `.package(url: "https://github.com/MapConductor/ios-geojson-layer", from: "${version}")`;
---

<Code code={code} lang="swift" />
